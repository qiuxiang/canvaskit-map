{
  "author": "7c00 <i@7c00.cc>",
  "scripts": {
    "core-build": "cd packages/core && esbuild build.ts --format=cjs | node && tsc",
    "core-dev": "cd packages/core && esbuild build.ts --format=cjs | node - dev",
    "react-build": "cd packages/react && esbuild build.ts --format=cjs | node && tsc",
    "react-dev": "cd packages/react && esbuild build.ts --format=cjs | node - dev"
  },
  "devDependencies": {
    "@types/node": "^20.6.3",
    "chokidar": "^3.5.3",
    "esbuild": "^0.19.2",
    "sucrase": "^3.34.0",
    "typescript": "^5.2.2",
    "vite": "^4.4.9"
  }
}
