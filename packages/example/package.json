{
  "scripts": {
    "build": "esbuild --bundle --external:fs --external:path --outdir=dist src/index.ts",
    "dev": "pnpm run build --watch --sourcemap --serve --servedir=dist"
  },
  "dependencies": {
    "@canvaskit-tilemap/core": "workspace:~"
  }
}
